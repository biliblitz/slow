// === SHARE ===
export { createContext, render } from "preact";
export type {
  ComponentChildren,
  ComponentType,
  Context,
  JSX,
  VNode,
} from "preact";

export {
  useCallback,
  useContext,
  useEffect,
  useMemo,
  useRef,
  useState,
} from "preact/hooks";

export {
  batch,
  signal,
  useComputed,
  useSignal,
  useSignalEffect,
} from "@preact/signals";
export type { ReadonlySignal, Signal } from "@preact/signals";
